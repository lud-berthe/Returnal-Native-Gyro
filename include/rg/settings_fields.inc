// type, name, default, minimum, maximum, description
RG_SETTING(bool, GyroEnabled, true, 0, 1, "Enable native angular gyro camera input.")
RG_SETTING(int, GyroInputMode, 0, 0, 1, "0 Auto; 1 Native. Both require a native sensor; never generates mouse input.")
RG_SETTING(int, SensorBackend, 0, 0, 4, "Legacy value retained for compatibility; runtime chooses the source automatically at launch. Restart Returnal after changing Steam Input. Disable gyro output in the Steam layout.")
RG_SETTING(int, DeviceIndex, 0, 0, 15, "Motion-device index: Steam gamepad slot/handle order, direct devices by path. Restart to change.")
RG_SETTING(float, SensitivityX, 2.5f, 0, 10, "Horizontal natural sensitivity: 1 means one camera degree per controller degree.")
RG_SETTING(float, SensitivityY, 2.5f, 0, 10, "Vertical natural sensitivity, independent of horizontal sensitivity.")
RG_SETTING(bool, LinkXY, false, 0, 1, "Legacy key, ignored. X and Y now remain independent.")
RG_SETTING(float, AimMultiplier, 1.0f, 0, 10, "Additional multiplier in verified aiming state.")
RG_SETTING(float, AltFireMultiplier, 1.0f, 0, 10, "Additional multiplier in verified Alt-Fire state, replacing AimMultiplier.")
RG_SETTING(int, GyroSpace, 0, 0, 3, "0 Player Space; 1 Local Yaw; 2 Local Roll; 3 World Space.")
RG_SETTING(int, ActivationMode, 1, 0, 5, "0 Always On (hold common button to disable); 1 Aim Only; 2 Hip-fire Only; 3 Hold Enable; 5 Toggle. Legacy 4 migrates to 0.")
RG_SETTING(int, ActivationButton, 0, 0, 28, "0 None; 1 L1; 2 R1; 3 L3; 4 R3; 5 Touchpad contact; 7 Circle/East; 10 Square/West; 11 Triangle/North; 12/13 left/right trackpad; 14/15/16 left/right/either thumbstick touch; 17/18/19 left/right/either grip touch; 20/21 L4/R4; 22/23 L5/R5; 24 both pads; 25 both stick sensors; 26 both grip sensors; 27/28 either/both stick clicks. Game actions use taps shorter than 200 ms; gyro responds immediately. Legacy IDs 6/8/9 reset to None.")
RG_SETTING(int, GyroButton, 0, 0, 23, "Gyro physical button: 0 Off; 1 L1; 2 R1; 3 L3; 4 R3; 7 East; 10 West; 11 North; 20/21 L4/R4; 22/23 L5/R5.")
RG_SETTING(int, GyroTouchpad, 0, 0, 24, "Gyro trackpad contact: 0 Off; 12 Left; 13 Right; 5 Either or single touchpad; 24 Both.")
RG_SETTING(int, GyroStickSensor, 0, 0, 25, "Gyro thumbstick contact: 0 Off; 14 Left; 15 Right; 16 Either; 25 Both.")
RG_SETTING(int, GyroGripSensor, 0, 0, 26, "Gyro grip contact: 0 Off; 17 Left; 18 Right; 19 Either; 26 Both.")
RG_SETTING(int, GyroStick, 0, 0, 28, "Gyro stick deflection: 0 Off; 3 Left; 4 Right; 27 Either; 28 Both. Enabled families combine with OR.")
RG_SETTING(int, RatchetButton, 0, 0, 8, "Legacy input alias. The shared ActivationButton now handles activation and ratcheting.")
RG_SETTING(bool, DisableWhileRightStick, false, 0, 1, "Suspend gyro while right-stick magnitude exceeds RightStickThreshold.")
RG_SETTING(float, RightStickThreshold, 0.2f, 0, 1, "Normalized deflection threshold for either stick in the GyroStick family.")
RG_SETTING(bool, InvertX, false, 0, 1, "Invert gyro horizontal direction independently of game controls.")
RG_SETTING(bool, InvertY, false, 0, 1, "Invert gyro vertical direction independently of game controls.")
RG_SETTING(int, AutomaticCalibration, 0, 0, 2, "0 Off; 1 Anytime (legacy enabled); 2 Menus only while stationary. Steam Input owns its calibration.")
RG_SETTING(float, CalibrationSeconds, 3.0f, 1, 10, "Manual still-surface calibration duration. Movement restarts collection.")
RG_SETTING(float, TighteningDps, 0.0f, 0, 20, "Optional continuous precision threshold in deg/s: scales input by min(speed/threshold,1), not a deadzone.")
RG_SETTING(int, Smoothing, 0, 0, 500, "Exponential smoothing time constant in milliseconds, 0 Off to 500, in steps of 5.")
RG_SETTING(float, SmoothingThresholdDps, 2.0f, 0.01, 100, "Legacy value, ignored by exponential smoothing.")
RG_SETTING(float, SmoothingSeconds, 0.04f, 0.001, 0.25, "Legacy value, ignored. Smoothing now sets the time constant in milliseconds.")
RG_SETTING(float, CutoffDps, 0.0f, 0, 10, "Optional hard angular deadzone. Zero preserves arbitrarily small motion.")
RG_SETTING(int, Acceleration, 0, 0, 3, "0 Off; 1 Low (up to 1.5x); 2 Medium (2x); 3 High (3x). Multiplies each axis independently.")
RG_SETTING(float, MinSensitivity, 1.0f, 0, 10, "Natural sensitivity below AccelerationStartDps.")
RG_SETTING(float, MaxSensitivity, 1.0f, 0, 10, "Natural sensitivity at or above AccelerationEndDps.")
RG_SETTING(float, AccelerationStartDps, 0.0f, 0, 1000, "Controller rotation speed where acceleration begins (deg/s).")
RG_SETTING(float, AccelerationEndDps, 75.0f, 0.01, 2000, "Controller rotation speed where acceleration reaches MaxSensitivity (deg/s).")
RG_SETTING(bool, MixedInput, true, 0, 1, "Enable the verified mouse-motion input-method transition guard.")
RG_SETTING(bool, IgnoreMouseMotion, true, 0, 1, "Mouse motion still reaches camera/UI, but does not change presentation during mixed input.")
RG_SETTING(int, HudMode, 0, 0, 2, "0 Automatic except ignored mouse movement; 1 Controller; 2 Keyboard/Mouse.")
RG_SETTING(bool, DiagnosticLogging, true, 0, 1, "Log lifecycle, binding validation, controller and calibration changes.")
RG_SETTING(bool, DebugOverlay, false, 0, 1, "Show compact sensor and camera diagnostics in the configuration panel.")
RG_SETTING(bool, VerboseSamples, false, 0, 1, "Record per-sample sensor diagnostics; large logs and higher overhead, debugging only.")

RG_SETTING(float, UIScale, 0.0f, 0, 3, "Settings text/control scale: 0 Auto (DPI and resolution), 1 to 3 explicit scale. Auto uses 2x on 4K.")

RG_SETTING(int, ConfigVersion, 4, 1, 4, "Configuration schema version; old bindings and smoothing presets are migrated on load.")

RG_SETTING(int, FlickStick, 0, 0, 2, "0 Off; 1 Hip-fire only (not aim or Alt-Fire); 2 On. Replaces normal right-stick camera input during gameplay.")
RG_SETTING(int, FlickSpinDurationMs, 150, 0, 1000, "Initial flick rotation duration in milliseconds; 0 is instant. Independent from continuous stick rotation.")
